apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: artifactory-postgres-data
  name: artifactory-postgres-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
